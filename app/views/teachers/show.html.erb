<h1><%= @teacher.name %></h1>
<p>
This Teacher belongs to <%= link_to @teacher.house.name, house_path(@teacher.house) %>
</p>
<h2>Students of <%= @teacher.name %> : </h2>
<% if @teacher.house.students.empty?%>
  <p>There are no Student assigned to <%= @teacher.name%></p>
<%else%>
  <% @teacher.house.students.each do |student|%>
    <li>
    <%= link_to student.name, student_path(student)%>
    </li>
  <%end%>
<%end%>

<h2>Students who have a Class scheduled with : <%= @teacher.name%></h2>
<% if @teacher.school_class.empty?%>
   <p>There no class scheduled currently :)</p>
<%else%>
  <% @teacher.school_class.each do |school|%>
    <li>
    <%= link_to school.student.name, school.student %> at 
    <%= school.class_time %>
    </li>
    <%end%> 
<%end%>